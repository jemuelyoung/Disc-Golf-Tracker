<!DOCTYPE html>
<html>
	<head>
		<title>Jem's Disc Golf Application</title>
		<%= stylesheet_link_tag "bootstrap.min", "application" %>
		<%= stylesheet_link_tag "bootstrap-responsive.min", :media => "all" %>
		<%= javascript_include_tag "bootstrap.min" %>
		<%= csrf_meta_tags %>
	</head>
	<body>
		<%= render 'layouts/navbar' %>
		<div class="container">
			<div class="hero-unit">
				<center>
					<%= yield %>
				</center>
			</div>
		
		<footer>
			<p >Built with love by Jem</p>
		</footer>
		</div>	
	</body>
</html>
